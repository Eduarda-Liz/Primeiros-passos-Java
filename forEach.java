/* for-each — forma simplificada de percorrer arrays */
for (tipo elemento : array) {
    // usa o elemento diretamente
}

int[] numeros = {10, 20, 30};
for (int n : numeros) {
    System.out.println(n);
}
